import React from "react";
import {Box} from "grommet";

const AccountScreenContainer = ({form}) => (
    <Box  pad="small"  margin="medium" fill>
        {form}
    </Box>
)

export default AccountScreenContainer;