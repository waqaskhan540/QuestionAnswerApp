import React from "react";
import {Box} from "grommet";

const LoginScreenContainer = ({form}) => (
    <Box margin="large" pad="small" width="medium">
        {form}
    </Box>
)

export default LoginScreenContainer;